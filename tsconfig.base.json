{
  "compilerOptions": {
    "baseUrl": ".",
    "target": "ESNext", // 指定编译后 JavaScript 的版本，这里是最新的 ES 标准
    "module": "ESNext", // 指定使用的模块系统，这里是 ES Module（import/export
    "moduleResolution": "Node", // 模块解析策略，Node 风格（会从 node_modules 里找依赖）
    "lib": ["ESNext", "DOM"], // 编译时包含的库，ES 最新特性 + DOM API
    "allowJs": false, // 是否允许编译 .js 文件（false 只编译 .ts / .tsx）
    "checkJs": false, // 是否检查 .js 文件的类型错误（通常和 allowJs 一起用）
    "jsx": "react-jsx", // React 17+ 新的 JSX 转换方式（无需 import React）
    "strict": true, // 开启所有严格模式选项（强类型检查）
    "esModuleInterop": true, // 允许 import/export CommonJS 模块时更友好
    "skipLibCheck": true, // 跳过对 .d.ts 类型声明文件的检查（提升编译速度）
    "forceConsistentCasingInFileNames": true, // 文件名大小写必须一致（避免跨平台问题）
    "isolatedModules": true, // 每个文件单独作为模块（适合构建工具，避免跨文件依赖）
    "resolveJsonModule": true, // 允许 import JSON 文件
    "allowSyntheticDefaultImports": true, // 允许从没有默认导出的包中默认导入
    "noEmit": false // 是否输出编译结果（false 表示会生成编译后的文件）
  }
}
